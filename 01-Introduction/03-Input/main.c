#include <stdio.h>

int main() {

    // Declare number as integer
    int num;
    printf("Enter a number: ");
    // Reads an integer number
    scanf("%d", &num);
    // Displays the output
    printf("Your number is: %d\n", num);
    return 0;
}
